{
  "name": "@q/agent-core",
  "version": "0.1.0",
  "private": true,
  "description": "Qode — Core agent engine: agent loop, tool execution, sub-agents",
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "types": "./dist/index.d.mts"
    }
  },
  "files": ["dist/"],
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@q/qprovs": "workspace:*",
    "@q/qmain": "workspace:*",
    "@q/telemetry": "workspace:*",
    "pathe": "^2.0.0",
    "zod": "^4.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "tsdown": "^0.22.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}