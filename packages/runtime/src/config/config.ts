/**
 * ConfigManager — Configuration manager with three-tier TOML loading.
 *
 * Manages configuration across user-global (~/.Q/config.toml), project-level
 * (.q/config.toml), and session-level (.q/session.toml) tiers.
 *
 * @deprecated Use ConfigStore from ./store.js instead.
 */
export class ConfigManager {
  constructor() {
    // Config manager placeholder
  }
}
